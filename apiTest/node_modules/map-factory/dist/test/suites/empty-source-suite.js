"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _code = require("code");

var _labTesting = require("lab-testing");

var _labTesting2 = _interopRequireDefault(_labTesting);

var _labSuite = require("lab-suite");

var labSuite = _interopRequireWildcard(_labSuite);

var _mapFactory = require("../../lib/map-factory");

var _mapFactory2 = _interopRequireDefault(_mapFactory);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

var suite = labSuite.create();

suite.expect("LABELS").to.be.an.array();
suite.expect("GET_ITEM").to.be.a.string();
suite.expect("SET_ITEM").to.be.a.string();
suite.expect("NO_SOURCE_EXPECTED").to.be.anything();
suite.expect("MODIFY_VALUE").to.be.anything();
suite.expect("MODIFIED_EXPECTED").to.be.anything();
suite.expect("EXPERIMENTAL").to.be.a.boolean();

suite.declare(function (lab, variables) {
  var LABELS = variables.LABELS,
      GET_ITEM = variables.GET_ITEM,
      SET_ITEM = variables.SET_ITEM,
      NO_SOURCE_EXPECTED = variables.NO_SOURCE_EXPECTED,
      MODIFY_VALUE = variables.MODIFY_VALUE,
      MODIFIED_EXPECTED = variables.MODIFIED_EXPECTED,
      EXPERIMENTAL = variables.EXPERIMENTAL;


  var testing = (0, _labTesting2.default)(lab);
  var group = testing.createExperiment.apply(testing, _toConsumableArray(LABELS));

  function createSut() {
    return (0, _mapFactory2.default)({ experimental: EXPERIMENTAL });
  }

  // const experimentalLabel = EXPERIMENTAL === true ? "in experimental mode" : "in normal mode";

  group("when source does not exist", function () {

    // lab.experiment(experimentalLabel, () => {

    lab.test("the target field does not get created for a basic map", function () {

      var mapper = createSut();

      var actual = mapper.map(GET_ITEM).to(SET_ITEM).execute({});

      (0, _code.expect)(actual).to.equal(NO_SOURCE_EXPECTED);
    });

    lab.test("the target field does not get created with a modifying transform", function () {

      var mapper = createSut();

      var actual = mapper.map(GET_ITEM).to(SET_ITEM, function () {
        return MODIFY_VALUE;
      }).execute({});

      (0, _code.expect)(actual).to.equal(NO_SOURCE_EXPECTED);
    });

    lab.test("the target field does get created with a modifying transform with always flag", function () {

      var mapper = createSut();

      var actual = mapper.map(GET_ITEM).always.to(SET_ITEM, function () {
        return MODIFY_VALUE;
      }).execute({});

      (0, _code.expect)(actual).to.equal(MODIFIED_EXPECTED);
    });

    lab.test("the target field does not get created for an array source with a pass-through transform", function () {

      var mapper = createSut();

      var actual = mapper.map([GET_ITEM]).to(SET_ITEM, function (value) {
        return value;
      }).execute({});

      (0, _code.expect)(actual).to.equal(NO_SOURCE_EXPECTED);
    });

    lab.test("the target field does get created for an array source with a modifying transform with always flag", function () {

      var mapper = createSut();

      var actual = mapper.map([GET_ITEM]).always.to(SET_ITEM, function () {
        return MODIFY_VALUE;
      }).execute({});

      (0, _code.expect)(actual).to.equal(MODIFIED_EXPECTED);
    });

    lab.test("the target field does not get created for an array source with a modifying transform", function () {

      var mapper = createSut();

      var actual = mapper.map([GET_ITEM]).to(SET_ITEM, function () {
        return MODIFY_VALUE;
      }).execute({});

      (0, _code.expect)(actual).to.equal(NO_SOURCE_EXPECTED);
    });
  });
  // });
});

exports.default = suite;